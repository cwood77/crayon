load-image r"nerd.bmp" :
   select-object 0 :
      crop
   snip -90 -> rotated

new-image rect[tl,br]{pnt{0,0},pnt{1000,1000}} :
   overlay $rotated
   select-object 0 :
      crop
   save-image r"out-rotatedNerd.bmp"
